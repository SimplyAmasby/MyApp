Run on ios simulator

1. Install yarn dependencies.
cd MyApp
yarn

2. Install pod files.
cd ios
pod install

3. Run ios in simulator
yarn ios

Run on android emulator

1. Install yarn dependencies.
cd MyApp
yarn

3. Run ios in simulator
yarn android
